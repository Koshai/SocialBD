# QueueOra — worker service (Railway)
# Settings → Config-as-code → path: railway.worker.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
